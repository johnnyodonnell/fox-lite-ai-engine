# Install with:  pip install -r requirements.txt
#
# Training runs on asus-nvidia: Ubuntu 24.04, aarch64, NVIDIA GB10 (Blackwell,
# sm_121), CUDA 13.0. PyTorch's stable wheels don't yet ship cu130 for aarch64,
# so we pull the nightly cu130 wheel. It's the only torch build that exposes the
# GB10 to CUDA on this machine.
--pre
--index-url https://download.pytorch.org/whl/nightly/cu130
--extra-index-url https://pypi.org/simple
torch
numpy
tqdm
