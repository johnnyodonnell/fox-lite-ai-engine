numpy>=2.0
onnx>=1.20
onnxruntime>=1.20
onnxscript>=0.5
safetensors>=0.4
# torch is installed separately from the PyTorch nightly cu128 index (matches the
# chess-ai-engine setup on asus-nvidia / GB10):
#   uv pip install --index-url https://download.pytorch.org/whl/nightly/cu128 torch
